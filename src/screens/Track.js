import { View, Text } from "react-native";
import React from "react";

const Track = () => {
  return (
    <View>
      <Text>Only Available on ios and android </Text>
    </View>
  );
};

export default Track;
